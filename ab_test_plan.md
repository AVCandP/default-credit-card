# A/B Test Plan — Credit Card Default Prediction

## 1. Цель эксперимента

Определить, обеспечивает ли модель v2 (RandomForest) статистически значимое улучшение F1-скора по классу «дефолт» по сравнению с моделью v1 (LogisticRegression).

---

## 2. Гипотезы

| | |
|---|---|
| **H₀** | F1(v2) ≤ F1(v1) — модель v2 не лучше контроля |
| **H₁** | F1(v2) > F1(v1) — модель v2 значимо лучше |

---

## 3. Характеристики моделей (на тестовой выборке, 6 000 объектов)

| Метрика | v1 LogisticRegression | v2 RandomForest |
|---|---|---|
| F1 (default class) | **0.4612** | 0.4484 |
| Precision | 0.37 | **0.64** |
| Recall | **0.62** | 0.34 |
| Accuracy | 0.68 | **0.81** |

**Интерпретация:**
- v1 — высокий Recall: ловит больше реальных дефолтов (меньше FN), но чаще ошибочно отказывает хорошим клиентам
- v2 — высокая Precision: меньше ложных тревог (меньше FP), но пропускает часть реальных дефолтов

Выбор «лучшей» модели зависит от бизнес-контекста (стоимость FP vs FN).

---

## 4. Метрики оценки

### Первичная метрика
**F1-score для класса «дефолт» (класс 1)**

Обоснование: при дисбалансе классов (~22 % дефолтов) accuracy вводит в заблуждение. F1 балансирует Precision и Recall, учитывая оба типа ошибок.

### Вторичные метрики
1. **Precision** — при росте операционных затрат на ручную проверку подозрительных клиентов
2. **Снижение ожидаемых кредитных потерь** (бизнес-метрика):

```
Δ_loss = (FN_v1 - FN_v2) × avg_exposure_NT$
```

Если v2 перехватывает больше дефолтов — разница в ожидаемых потерях конвертируется в деньги.

---

## 5. Дизайн эксперимента

### Схема трафика

```
Входящий запрос
      │
      ▼
  model_version = "ab"
      │
      ▼
  hash(client_id) % 2
      │
   0  │  1
   ▼     ▼
  v1    v2
(50%) (50%)
```

Детерминированное 50/50-разбиение по `hash(client_id)` — один клиент всегда попадает в одну ветку, исключая эффект новизны.

### Активация A/B-режима в API

```bash
curl -X POST http://localhost:5000/predict \
  -H "Content-Type: application/json" \
  -d '{ ...features..., "model_version": "ab", "client_id": "client-00123" }'
```

Ответ содержит поле `model_version`, позволяющее логировать, какая версия обработала запрос.

---

## 6. Статистические параметры

| Параметр | Значение |
|---|---|
| Уровень значимости α | 0.05 |
| Мощность теста 1−β | 0.80 |
| Минимальный детектируемый эффект (MDE) | ΔMDE = +0.02 по F1 |
| Метод | Двусторонний z-тест для разности долей |
| Размер выборки (на ветку) | ~2 000 наблюдений |
| Минимальная длительность | 2 недели |
| Рекомендуемая длительность | 4 недели |

### Формула z-теста

```
z = (p̂₂ - p̂₁) / sqrt(p̂(1-p̂)(1/n₁ + 1/n₂))

где p̂ = (x₁ + x₂) / (n₁ + n₂)  — pooled proportion
```

Доверительный интервал для разности:

```
CI = (p̂₂ - p̂₁) ± z_{α/2} × SE
```

---

## 7. Критерии завершения

| Условие | Действие |
|---|---|
| p-value < 0.05 И Δ ≥ MDE | Переключить 100 % трафика на v2 |
| p-value ≥ 0.05 через 4 недели | Оставить v1, закрыть эксперимент |
| Precision(v2) < Precision(v1) − 0.03 | Досрочно остановить тест, расследовать |
| Количество ошибок API v2 > 1 % | Немедленно откатить v2 |

---

## 8. Логирование для пост-анализа

Каждый запрос в `model_version=ab`-режиме логируется в JSON:

```json
{
  "event": "prediction",
  "model_version": "v2",
  "client_id": "client-00123",
  "prediction": 1,
  "probability": 0.87,
  "latency_ms": 12.4,
  "timestamp": "2026-05-02T16:44:57Z"
}
```

После окончания периода сбора данных: разметить `true_label` (факт дефолта из биллинговой системы) и рассчитать F1/Precision/Recall по каждой ветке.
